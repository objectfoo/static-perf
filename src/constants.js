// role API will serve a json of with random data
exports.API = 'API';

// role HELLO will serve page that first does some API requests
exports.HELLO = 'HELLO';

// role STATIC will serve up some static assets, img script etc.
exports.STATIC = 'STATIC';

